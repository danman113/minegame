import { Polygon, pt } from '../../src/math'
import { expect } from 'chai'

describe('polygon', function () {
  it('Polygon works with 3 verticies', function () {
    const p1 = new Polygon(pt(0, 0), pt(0, 0), pt(0, 0))
  })
})
